spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
  cloud:
    discovery:
      enabled: false
spring-doc:
  api-docs:
    enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false